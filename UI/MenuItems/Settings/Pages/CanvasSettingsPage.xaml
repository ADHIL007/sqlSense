<UserControl x:Class="sqlSense.UI.MenueItems.Settings.Pages.CanvasSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../SettingsResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Margin="0,0,0,0">
        <TextBlock Text="Canvas" Style="{StaticResource SectionHeader}"/>

        <StackPanel Margin="0,0,0,18">
            <CheckBox Content="Show grid dots" 
                      Style="{StaticResource SettingCheckBox}" IsChecked="True"/>
            <TextBlock Text="Display the dot pattern on the canvas background." 
                       Style="{StaticResource SettingDescription}" Margin="24,4,0,0"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,18">
            <CheckBox Content="Snap to grid" 
                      Style="{StaticResource SettingCheckBox}"/>
            <TextBlock Text="Snap table cards to the nearest grid point when dragging." 
                       Style="{StaticResource SettingDescription}" Margin="24,4,0,0"/>
        </StackPanel>

        <Grid Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Default zoom level (%):" Style="{StaticResource SettingLabel}"/>
            <TextBox Grid.Column="1" Text="100" Style="{StaticResource SettingTextBox}"/>
        </Grid>
    </StackPanel>
</UserControl>
