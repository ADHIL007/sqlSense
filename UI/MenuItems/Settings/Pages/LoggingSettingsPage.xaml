<UserControl x:Class="sqlSense.UI.MenueItems.Settings.Pages.LoggingSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../SettingsResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,0">
            <TextBlock Text="Logging &amp; Diagnostics" Style="{StaticResource SectionHeader}"/>

            <StackPanel Margin="0,0,0,18">
                <CheckBox x:Name="CbEnableLogging" Content="Enable HTTP Traffic Logging" 
                          Style="{StaticResource SettingCheckBox}" IsChecked="True"/>
                <TextBlock Text="Capture all outgoing HTTP requests and responses to a local file for debugging purposes." 
                           Style="{StaticResource SettingDescription}" Margin="24,4,0,0"/>
            </StackPanel>

            <Border Height="1" Background="#3C3C3C" Margin="0,8,0,24"/>

            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="Log Storage" Style="{StaticResource SettingLabel}" Margin="0,0,0,10"/>
                <TextBlock x:Name="TbLogPath" Text="C:\Users\...\AppData\Local\sqlSense\logs" 
                           Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,0,12"/>
                
                <StackPanel Orientation="Horizontal">
                    <Button Content="Open Log Folder" Margin="0,0,10,0" Padding="12,6" Background="#333333" Foreground="White" BorderThickness="0" Cursor="Hand" Click="OpenLogFolder_Click"/>
                    <Button Content="Clear All Logs" Padding="12,6" Background="#442222" Foreground="#FF9999" BorderThickness="0" Cursor="Hand" Click="ClearLogs_Click"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Margin="0,0,0,18">
                <TextBlock Text="Note: Logging can consume disk space over time. It is recommended to enable it only when troubleshooting issues." 
                           Style="{StaticResource SettingDescription}" Foreground="#FFA000"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
