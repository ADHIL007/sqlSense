<UserControl x:Class="sqlSense.UI.SqlEditorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="300">
    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Margin="15,15,15,5">
                <TextBlock Text="SQL EDITOR" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Maximize / Restore Toggle -->
                    <Button Command="{Binding SqlEditor.ToggleMaximizedCommand}" FontFamily="Segoe MDL2 Assets" 
                            Content="&#xE943;" ToolTip="Maximize Code Editor"
                            Background="Transparent" BorderThickness="0" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,8,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- Close -->
                    <Button Command="{Binding SqlEditor.CloseCommand}" FontFamily="Segoe MDL2 Assets" 
                            Content="&#xE8BB;" ToolTip="Close Editor"
                            Background="Transparent" BorderThickness="0" Foreground="{StaticResource MutedBrush}" FontSize="9">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
            <TextBox Grid.Row="1" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource SuccessBrush}" 
                     BorderThickness="0" Padding="10" AcceptsReturn="True" AcceptsTab="True" FontFamily="Consolas"
                     VerticalScrollBarVisibility="Auto"
                     Text="{Binding SqlEditor.SqlText, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
    </Border>
</UserControl>
