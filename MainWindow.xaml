<Window x:Class="sqlSense.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:sqlSense"
        xmlns:models="clr-namespace:sqlSense.Models"
        xmlns:converters="clr-namespace:sqlSense.Converters"
        mc:Ignorable="d"
        Title="sqlSense - Database Visualization" Height="800" Width="1280"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <!-- TreeView Item Toggle Button (expand/collapse arrow) -->
        <Style x:Key="ExpandCollapseToggle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent" Padding="2">
                            <Path x:Name="Arrow" Fill="#888888" 
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Data="M 0 0 L 5 4 L 0 8 Z"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Arrow" Property="Data" Value="M 0 0 L 8 0 L 4 5 Z"/>
                                <Setter TargetName="Arrow" Property="Fill" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Arrow" Property="Fill" Value="#4FC3F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TreeViewItem Container Style -->
        <Style TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="2,1"/>
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="ToolTip" Value="{Binding Tooltip}"/>
            <EventSetter Event="RequestBringIntoView" Handler="TreeViewItem_RequestBringIntoView"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <StackPanel>
                            <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" 
                                    BorderThickness="0" Padding="0,1">
                                <Grid Margin="{Binding Converter={x:Static local:TreeIndentConverter.Instance}, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="18"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="Expander" Grid.Column="0"
                                                  Style="{StaticResource ExpandCollapseToggle}"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"/>
                                    <ContentPresenter x:Name="PART_Header" Grid.Column="1"
                                                      ContentSource="Header" Margin="2,0,0,0"
                                                      VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#094771"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2A2D2E"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Converters -->
        <converters:StringToBrushConverter x:Key="StringToBrush"/>

        <!-- HierarchicalDataTemplate for tree items with icons -->
        <HierarchicalDataTemplate DataType="{x:Type models:DatabaseTreeItem}" 
                                   ItemsSource="{Binding Children}">
            <Grid Margin="1,1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="22"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{Binding IconGlyph}" 
                           FontFamily="Segoe MDL2 Assets" FontSize="14"
                           Foreground="{Binding IconColor, Converter={StaticResource StringToBrush}}"
                           VerticalAlignment="Center" HorizontalAlignment="Center"
                           Opacity="0.9"/>
                           
                <TextBlock Grid.Column="1" Text="{Binding Name}" 
                           VerticalAlignment="Center" FontSize="12.5"
                           Margin="4,0,0,0"/>
                           
                <TextBlock Grid.Column="2" Text=" (loading...)" 
                           FontSize="10" Foreground="#888888" 
                           VerticalAlignment="Center" Margin="6,0,0,0"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </HierarchicalDataTemplate>

        <!-- Dot Pattern Background Brush -->
        <DrawingBrush x:Key="DotPatternBrush" TileMode="Tile"
                      Viewport="0,0,20,20" ViewportUnits="Absolute"
                      Viewbox="0,0,20,20" ViewboxUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#1E1E1E">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0,0,20,20"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#333333">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry Center="10,10" RadiusX="1" RadiusY="1"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>

        <!-- Zoom Button Style -->
        <Style x:Key="ZoomButton" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#3E3E42"/>
                                <Setter TargetName="bg" Property="BorderBrush" Value="#4FC3F7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#094771"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pagination Button Style -->
        <Style x:Key="PaginationButton" TargetType="Button">
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bg" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Styles -->
        <Style x:Key="DarkDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#252526"/>
            <Setter Property="AlternatingRowBackground" Value="#2D2D30"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="True"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="RowHeight" Value="24"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#4FC3F7"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#094771"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2D2E"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#094771"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Menu Bar -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel LastChildFill="False" Margin="10,0">
                <Menu Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center">
                    <MenuItem Header="File" />
                    <MenuItem Header="Edit" />
                    <MenuItem Header="View" />
                    <MenuItem Header="Run" />
                    <MenuItem Header="Debug" />
                    <MenuItem Header="Tools" />
                </Menu>
                <Separator Background="{StaticResource BorderBrush}" Margin="10,5" Width="1"/>
                <Button Command="{Binding RunQueryCommand}" Content="▶ Execute" Background="Transparent" Foreground="{StaticResource SuccessBrush}" BorderThickness="0" Margin="5,0" VerticalAlignment="Center"/>
                <Button Command="{Binding ShowMetadataCommand}" Content="⚙ Sync" Background="Transparent" Foreground="{StaticResource WarningBrush}" BorderThickness="0" Margin="5,0" VerticalAlignment="Center"/>
                
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedBrush}" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="10,0" FontSize="11"/>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="180" MaxWidth="450"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300" MinWidth="200" MaxWidth="500"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT PANEL: Object Explorer -->
            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Panel Header -->
                    <Border Background="#2D2D30" Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE968;" FontFamily="Segoe MDL2 Assets" FontSize="14" 
                                       Foreground="#4FC3F7" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Object Explorer" FontWeight="SemiBold" Foreground="#CCCCCC" 
                                       FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Tree View -->
                    <TreeView x:Name="ObjectExplorer" Grid.Row="1" 
                              Background="Transparent" BorderThickness="0" 
                              Foreground="{StaticResource TextSecondaryBrush}" 
                              Margin="0" Padding="4"
                              ItemsSource="{Binding TreeItems}"
                              TreeViewItem.Expanded="TreeViewItem_Expanded"
                              SelectedItemChanged="ObjectExplorer_SelectedItemChanged"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Border>

            <!-- CENTER PANEL: Infinite Canvas with Dot Pattern -->
            <Border Grid.Column="1" Background="#1E1E1E" ClipToBounds="True">
                <Grid>
                    <!-- Canvas Container (handles pan/zoom) -->
                    <Border x:Name="CanvasContainer" Background="{StaticResource DotPatternBrush}"
                            ClipToBounds="True"
                            MouseWheel="Canvas_MouseWheel"
                            MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                            MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
                            MouseMove="Canvas_MouseMove"
                            MouseRightButtonDown="Canvas_MouseRightButtonDown"
                            MouseRightButtonUp="Canvas_MouseRightButtonUp">
                        
                        <!-- The actual infinite canvas surface -->
                        <Canvas x:Name="FlowCanvas" Width="10000" Height="10000"
                                Background="Transparent"
                                RenderTransformOrigin="0,0">
                            <Canvas.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform x:Name="CanvasScale" 
                                                    ScaleX="{Binding CanvasZoom}" 
                                                    ScaleY="{Binding CanvasZoom}"/>
                                    <TranslateTransform x:Name="CanvasTranslate" X="0" Y="0"/>
                                </TransformGroup>
                            </Canvas.RenderTransform>

                            <!-- Center cross-hair origin marker -->
                            <Line X1="4990" Y1="5000" X2="5010" Y2="5000" 
                                  Stroke="#333333" StrokeThickness="1"/>
                            <Line X1="5000" Y1="4990" X2="5000" Y2="5010" 
                                  Stroke="#333333" StrokeThickness="1"/>

                            <!-- Table Data Card (placed on canvas) -->
                            <Border x:Name="TableDataCard" Canvas.Left="4900" Canvas.Top="4950"
                                    Background="#252526" BorderBrush="#4FC3F7" BorderThickness="1"
                                    CornerRadius="6" MinWidth="300"
                                    Visibility="{Binding IsPreviewVisible, Converter={StaticResource BoolToVis}}">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="16" Opacity="0.5" ShadowDepth="4"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Card Header -->
                                    <Border Background="#4FC3F7" Padding="12,8" CornerRadius="5,5,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE80A;" FontFamily="Segoe MDL2 Assets" FontSize="14" 
                                                       Foreground="#1E1E1E" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding PreviewTableName}" FontSize="13" 
                                                       Foreground="#1E1E1E" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBlock Text=" — TOP 10" FontSize="10" 
                                                       Foreground="#1E1E1EAA" VerticalAlignment="Center" Margin="6,0,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- DataGrid -->
                                    <DataGrid Grid.Row="1" Style="{StaticResource DarkDataGrid}"
                                              ItemsSource="{Binding PagedData}"
                                              MaxHeight="200"/>

                                    <!-- Pagination Bar -->
                                    <Border Grid.Row="2" Background="#2D2D30" Padding="8,6"
                                            CornerRadius="0,0,5,5"
                                            BorderBrush="#3C3C3C" BorderThickness="0,1,0,0">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="◀" Style="{StaticResource PaginationButton}" 
                                                    Command="{Binding PreviousPageCommand}" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding PageInfo}" FontSize="10" 
                                                       Foreground="#888888" VerticalAlignment="Center" Margin="4,0"/>
                                            <Button Content="▶" Style="{StaticResource PaginationButton}" 
                                                    Command="{Binding NextPageCommand}" Margin="6,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </Canvas>
                    </Border>

                    <!-- Central Toolbar (bottom center, overlaid) -->
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Center" VerticalAlignment="Bottom"
                                Margin="0,0,0,24">
                        
                        <!-- Data Flow Toggle -->
                        <Border Background="#252526" CornerRadius="8" 
                                BorderBrush="#3C3C3C" BorderThickness="1"
                                Margin="0,0,16,0">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.5" ShadowDepth="4"/>
                            </Border.Effect>
                            <Button x:Name="DataFlowToggleBtn" 
                                    Content="&#xE14F; DATA FLOW: OFF" FontSize="13" 
                                    FontFamily="Segoe MDL2 Assets, Segoe UI"
                                    FontWeight="Bold" Foreground="#FF9800"
                                    Background="Transparent" BorderThickness="0"
                                    Padding="20,12" Cursor="Hand"
                                    Click="DataFlowToggleBtn_Click"/>
                        </Border>

                        <!-- Sync to DB -->
                        <Border Background="#252526" CornerRadius="8" 
                                BorderBrush="#3C3C3C" BorderThickness="1"
                                Margin="0,0,16,0">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.5" ShadowDepth="4"/>
                            </Border.Effect>
                            <Button x:Name="GenSqlBtn" 
                                    Content="&#xE943; GENERATE SQL" FontSize="13"
                                    FontFamily="Segoe MDL2 Assets, Segoe UI"
                                    FontWeight="Bold" Foreground="#4FC3F7"
                                    Background="Transparent" BorderThickness="0"
                                    Padding="20,12" Cursor="Hand"
                                    Command="{Binding GenerateViewSqlCommand}"/>
                        </Border>

                        <!-- Sync to DB -->
                        <Border Background="#252526" CornerRadius="8" 
                                BorderBrush="#3C3C3C" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.5" ShadowDepth="4"/>
                            </Border.Effect>
                            <Button x:Name="SyncDbBtn" 
                                    Content="&#xE105; SYNC TO DATABASE" FontSize="13"
                                    FontFamily="Segoe MDL2 Assets, Segoe UI"
                                    FontWeight="Bold" Foreground="#4CAF50"
                                    Background="Transparent" BorderThickness="0"
                                    Padding="20,12" Cursor="Hand"
                                    Command="{Binding SaveViewChangesCommand}"/>
                        </Border>
                    </StackPanel>

                    <!-- Zoom Controls (bottom right, overlaid) -->
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                Margin="0,0,16,16">
                        <Border Background="#252526" CornerRadius="6" 
                                BorderBrush="#3C3C3C" BorderThickness="1"
                                Padding="4">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="8" Opacity="0.4" ShadowDepth="2"/>
                            </Border.Effect>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="−" Style="{StaticResource ZoomButton}" 
                                        Command="{Binding ZoomOutCommand}" 
                                        FontSize="18" FontWeight="Bold"/>
                                <Border Background="#1E1E1E" CornerRadius="3" Padding="8,4" Margin="2,0"
                                        VerticalAlignment="Center" MinWidth="50">
                                    <TextBlock Text="{Binding ZoomPercentage}" 
                                               Foreground="#CCCCCC" FontSize="11"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               FontFamily="Consolas"/>
                                </Border>
                                <Button Content="+" Style="{StaticResource ZoomButton}" 
                                        Command="{Binding ZoomInCommand}"
                                        FontSize="16" FontWeight="Bold"/>
                                <Rectangle Width="1" Fill="#3C3C3C" Margin="4,4"/>
                                <Button Content="⟳" Style="{StaticResource ZoomButton}" 
                                        Command="{Binding ZoomResetCommand}"
                                        ToolTip="Reset to 100%" FontSize="14"/>
                                <Button Content="⤢" Style="{StaticResource ZoomButton}" 
                                        Command="{Binding ZoomFitCommand}"
                                        ToolTip="Zoom to fit" FontSize="14"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Canvas coordinate info (bottom left) -->
                    <Border Background="#252526EE" CornerRadius="4" Padding="8,4"
                            HorizontalAlignment="Left" VerticalAlignment="Bottom"
                            Margin="16,0,0,16" BorderBrush="#3C3C3C" BorderThickness="1">
                        <TextBlock x:Name="CoordinateLabel" Text="0, 0" 
                                   Foreground="#888888" FontSize="10" FontFamily="Consolas"/>
                    </Border>
                </Grid>
            </Border>

            <!-- RIGHT PANEL: SQL Editor -->
            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="SQL EDITOR" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" Margin="15,15,15,5" FontSize="11"/>
                    <TextBox Grid.Row="1" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource SuccessBrush}" 
                             BorderThickness="0" Padding="10" AcceptsReturn="True" AcceptsTab="True" FontFamily="Consolas"
                             VerticalScrollBarVisibility="Auto"
                             Text="{Binding SqlText, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Border>

            <!-- Resizers -->
            <GridSplitter Grid.Column="0" Width="4" Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
            <GridSplitter Grid.Column="1" Width="4" Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Stretch"/>
        </Grid>
    </Grid>
</Window>
